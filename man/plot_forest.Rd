% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_forest.R
\name{plot_forest}
\alias{plot_forest}
\title{Forest Plot of Study Effect Sizes With the Pooled Estimate}
\usage{
plot_forest(
  yi,
  vi,
  labels = NULL,
  method = c("reml", "pm", "dl", "fe"),
  hartung_knapp = TRUE,
  conf_level = 0.95,
  show_prediction = TRUE,
  xlab = "Effect size",
  title = NULL,
  palette = "okabe_ito",
  colors = NULL
)
}
\arguments{
\item{yi}{Numeric vector of study effect sizes.}

\item{vi}{Sampling variances of \code{yi}.}

\item{labels}{Optional study labels, one per study; defaults to
\code{"Study 1"}, \code{"Study 2"}, and so on, in the supplied order.}

\item{method, hartung_knapp}{Passed to \code{\link{meta_es}} for the
pooled row (\code{"reml"} and \code{TRUE} by default).}

\item{conf_level}{Confidence level for the per-study and pooled
intervals. Defaults to 0.95.}

\item{show_prediction}{Logical: draw the prediction interval band on the
pooled row? Default \code{TRUE} (ignored for \code{method = "fe"},
which has none).}

\item{xlab}{Label for the effect size axis. Defaults to
\code{"Effect size"}.}

\item{title}{Optional plot title.}

\item{palette}{Palette name. Defaults to \code{"okabe_ito"}, base R's
colorblind-safe Okabe-Ito palette; \code{"tableau"} is also available.}

\item{colors}{Optional length-2 vector overriding the palette: the study
color and the pooled-estimate color.}
}
\value{
A \pkg{ggplot} object; print it, or add further layers.
}
\description{
Draws the meta-analyst's central picture: every study's effect size with
its confidence interval, the random effects pooled estimate beneath them,
and, by default, the prediction interval showing where the effect of a
\emph{new} study is expected to land. Point sizes are proportional to
precision (inverse variance), so the eye weighs the studies the way the
model does. Requires \pkg{ggplot2}.
}
\examples{
# Twelve simulated studies whose true effects vary from study to study
# (between-study standard deviation 0.35), so the prediction interval
# for the effect of a new study is visibly wider than the confidence
# interval for the mean effect.
set.seed(113)
k <- 12
n <- sample(20:100, k)                    # per-group sample sizes
theta <- rnorm(k, mean = 0.4, sd = 0.35)  # true study effects
d <- rnorm(k, mean = theta, sd = sqrt(2 / n))
v <- 2 / n + d^2 / (4 * n)
plot_forest(d, v, xlab = "Standardized mean difference (d)")

# The teacher expectancy literature (Raudenbush, 1984): most studies
# cluster near zero, the estimated between-study variance is zero, and
# the prediction interval nearly coincides with the confidence interval.
data(teacher_expectancy)
d <- teacher_expectancy$d
n_e <- teacher_expectancy$n_experimental
n_c <- teacher_expectancy$n_control
v <- (n_e + n_c) / (n_e * n_c) + d^2 / (2 * (n_e + n_c))
plot_forest(d, v, labels = teacher_expectancy$author,
            xlab = "Standardized mean difference (d)")

}
\seealso{
\code{\link{meta_es}}, \code{\link{meta_smd}}, and
  \code{\link{meta_r}} for the numbers behind the picture;
  \code{\link{teacher_expectancy}} for the example data.

Other meta-analysis:
\code{\link[=combine_p]{combine_p()}},
\code{\link[=meta_contrast]{meta_contrast()}},
\code{\link[=meta_es]{meta_es()}},
\code{\link[=meta_r]{meta_r()}},
\code{\link[=meta_smd]{meta_smd()}}

Other plotting:
\code{\link[=plot_R2]{plot_R2()}},
\code{\link[=plot_cfa_k]{plot_cfa_k()}},
\code{\link[=plot_ci]{plot_ci()}},
\code{\link[=plot_equivalence]{plot_equivalence()}},
\code{\link[=plot_irt_information]{plot_irt_information()}},
\code{\link[=plot_mediation_mbco]{plot_mediation_mbco()}},
\code{\link[=plot_randomization_test]{plot_randomization_test()}},
\code{\link[=plot_regions_of_significance]{plot_regions_of_significance()}},
\code{\link[=plot_smd]{plot_smd()}},
\code{\link[=plot_trajectories]{plot_trajectories()}},
\code{\link[=plot_trajectories_fitted]{plot_trajectories_fitted()}},
\code{\link[=power_equivalence_md_plot]{power_equivalence_md_plot()}}
}
\author{
Ken Kelley \email{kkelley@nd.edu}
}
\concept{meta-analysis}
\concept{plotting}
\keyword{hplot}
