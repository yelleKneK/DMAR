% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss_power_c_ancova.R
\name{ss_power_c_ancova}
\alias{ss_power_c_ancova}
\title{Sample Size or Power for an Unstandardized Contrast in a One-Way ANCOVA}
\usage{
ss_power_c_ancova(
  psi,
  c_weights,
  sigma,
  rho,
  desired_power = 0.85,
  alpha_level = 0.05,
  n = NULL,
  directional = FALSE
)
}
\arguments{
\item{psi}{The population unstandardized contrast effect on the adjusted means, \eqn{\psi = \sum c_j \mu^{(adj)}_j}}

\item{c_weights}{Vector of contrast weights (must sum to zero); use fractional weights so the positive weights sum to 1}

\item{sigma}{Within-group population standard deviation of the response (the same \eqn{\sigma} as in a one-way ANOVA on the response)}

\item{rho}{Within-group population correlation between the response and the covariate; must lie in (-1, 1)}

\item{desired_power}{Desired statistical power (default 0.85)}

\item{alpha_level}{Type I error rate (default 0.05)}

\item{n}{Per-group sample size (assumed balanced); if specified, returns the realized power}

\item{directional}{Logical: \code{TRUE} for a one-sided test (in the same sign as \code{psi}), \code{FALSE} (default) for a two-sided test}
}
\value{
A \code{data.frame} with rows for \code{necessary_n_per_group} (or
  \code{specified_n_per_group}), \code{actual_power}, and \code{noncentral_t_parm}.
  The result carries the \code{dmar_ss_power} class, so
  \code{\link[generics]{tidy}} and \code{\link[generics]{glance}} summarize it
  in broom convention.
}
\description{
Determine the necessary per-group sample size to achieve a desired level of statistical power
for the test of a single planned (unstandardized) contrast on the adjusted means in a one-way
analysis of covariance, or, given a per-group sample size, return the realized statistical power.
}
\details{
This function uses the standard large-sample formulation in which the ANCOVA error variance is
\eqn{\sigma^2_{adj} = \sigma^2 (1 - \rho^2)}, the contrast \emph{t}-statistic has degrees of freedom
\eqn{N - J - 1} (one less than the corresponding ANOVA contrast because of the covariate), and the
noncentrality parameter is
\eqn{\lambda = \psi / (\sigma \sqrt{1 - \rho^2} \sqrt{\sum c_j^2 / n})}. This assumes the
covariate means are equal across groups (the typical assumption under random assignment); for
designs with substantial group differences in the covariate, the small-sample correction
\eqn{1 + (\bar X_{j} - \bar X_{\cdot})^2 / SS^{(within)}_X} would slightly inflate the standard
error and reduce power, an effect that is negligible for moderate or large \eqn{n}.
}
\examples{
# Same population contrast as in the ANOVA example, with rho = 0.5 between
# outcome and covariate; ANCOVA is more efficient than ANOVA here.
ss_power_c_ancova(psi = 0.5, c_weights = c(0.5, 0.5, -0.5, -0.5),
                  sigma = 1, rho = 0.5, desired_power = 0.80)

# Realized power for n = 30 per group
ss_power_c_ancova(psi = 0.5, c_weights = c(0.5, 0.5, -0.5, -0.5),
                  sigma = 1, rho = 0.5, n = 30)

}
\references{
Maxwell, S. E., Delaney, H. D., & Kelley, K. (2027). \emph{Designing experiments and analyzing data: A model comparison perspective} (4th ed.). Routledge.

Lai, K., & Kelley, K. (2012). Accuracy in parameter estimation for ANCOVA and ANOVA contrasts: Sample size planning via narrow confidence intervals. \emph{British Journal of Mathematical and Statistical Psychology, 65}, 350--370.
  \doi{10.1111/j.2044-8317.2011.02029.x}
}
\seealso{
\code{\link{ss_power_c}}, \code{\link{ci_c_ancova}}, \code{\link{ss_aipe_c_ancova}}

\code{\link{design_consequences}} for what a chosen design delivers:
  power, the Type S (sign) and Type M (exaggeration) errors of the
  significance filter, and the expected confidence interval width.

Other sample size for power:
\code{\link[=power_fisher_exact]{power_fisher_exact()}},
\code{\link[=ss_aipe_mixed_effects]{ss_aipe_mixed_effects()}},
\code{\link[=ss_power_R2]{ss_power_R2()}},
\code{\link[=ss_power_R2_sensitivity]{ss_power_R2_sensitivity()}},
\code{\link[=ss_power_c]{ss_power_c()}},
\code{\link[=ss_power_composite_ancova]{ss_power_composite_ancova()}},
\code{\link[=ss_power_composite_ancova_2group]{ss_power_composite_ancova_2group()}},
\code{\link[=ss_power_composite_anova]{ss_power_composite_anova()}},
\code{\link[=ss_power_composite_factorial_ancova]{ss_power_composite_factorial_ancova()}},
\code{\link[=ss_power_composite_factorial_ancova_het]{ss_power_composite_factorial_ancova_het()}},
\code{\link[=ss_power_composite_factorial_anova]{ss_power_composite_factorial_anova()}},
\code{\link[=ss_power_composite_sem]{ss_power_composite_sem()}},
\code{\link[=ss_power_contrast]{ss_power_contrast()}},
\code{\link[=ss_power_equivalence_c]{ss_power_equivalence_c()}},
\code{\link[=ss_power_factorial_ancova]{ss_power_factorial_ancova()}},
\code{\link[=ss_power_factorial_anova]{ss_power_factorial_anova()}},
\code{\link[=ss_power_indirect_effect]{ss_power_indirect_effect()}},
\code{\link[=ss_power_mixed_effects]{ss_power_mixed_effects()}},
\code{\link[=ss_power_one_way_anova]{ss_power_one_way_anova()}},
\code{\link[=ss_power_pcm]{ss_power_pcm()}},
\code{\link[=ss_power_r]{ss_power_r()}},
\code{\link[=ss_power_rc]{ss_power_rc()}},
\code{\link[=ss_power_reg_coef]{ss_power_reg_coef()}},
\code{\link[=ss_power_reg_coef_sensitivity]{ss_power_reg_coef_sensitivity()}},
\code{\link[=ss_power_rm_anova]{ss_power_rm_anova()}},
\code{\link[=ss_power_sc]{ss_power_sc()}},
\code{\link[=ss_power_sem]{ss_power_sem()}},
\code{\link[=ss_power_smd]{ss_power_smd()}},
\code{\link[=ss_power_split_plot_anova]{ss_power_split_plot_anova()}}
}
\author{
Ken Kelley \email{kkelley@nd.edu}
}
\concept{sample size for power}
\keyword{design}
\keyword{htest}
